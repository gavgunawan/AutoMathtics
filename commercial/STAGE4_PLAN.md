# Stage 4 — real provider, staging, real MFA, private pilot

Entered 9 Sep 2026 with Stages 1–3 closed for development at `release/v3.0 @ ab16a40`. Stage 4 works
*against* that foundation; nothing below redesigns it. "Closed for development" is not a public-production
certification — Stage 4 is where the external systems and real-user conditions get proven. The owner's
constraint stands: **zero cost for as long as possible**; every step that spends money is marked and needs
the owner's go.

| Workstream | What it proves | Cost / owner action |
|---|---|---|
| **4.0 foundation** | supported CI runtimes on immutable SHAs; self-service sign-in account deletion completing the parent lifecycle; this plan | none |
| **4.1 Stripe adapter** — built, unit-tested against a recorded Stripe surface | the Stage 3 gateway contract against a real provider: hosted checkout, provider idempotency keys, `Stripe-Signature` verification, price ids → plans, refund facts from the provider object, superseded sessions expired at the provider, provider state fetched where event order is not total (`PAYMENTS.md` → Stripe) | none to build; **Stripe account in test mode** (free, no fees) to exercise in 4.2 — owner creates it |
| **4.2 reconciliation** — code built and unit-tested (provider effects of cancel / scheduled downgrade / deletion, `reconcile-provider`, `resolve-event`, RECONCILIATION.md runbook and sandbox drill); **the sandbox drill itself waits for the owner's Stripe test account** | `reconciliation_required`, stale/frozen/superseded intents, provider-charged-but-local-finalisation-failed, webhook retries and out-of-order delivery, dunning, cancellations, refunds, reconciliation reports — with `stripe listen` forwarding real test-mode events to the local server | none (Stripe test mode + emulator) — owner creates the free test account and runs RECONCILIATION.md → sandbox drill with the operator |
| **4.3 staging** | a separate v3 Firebase project with production-equivalent IAM, Secret Manager, HTTPS origin, `TRUSTED_PROXY_HOPS` measured, TTL policies, logging, rollback; never connected to the v2 `automathtics` project | **Blaze billing account** (Cloud Run min 0, expected near-zero at pilot scale; budget alerts first) — owner's go |
| **4.4 real MFA + recovery** — policy and ceremony built (`RECOVERY.md`: self-service, seven-day wait, proof by the provider's own password reset, cancelled by any full sign-in, operator can only cancel; tested in memory and against the Auth emulator) | real SMS second factor (Identity Platform on the staging project) and the lost-phone / changed-number recovery ceremony that cannot be socially engineered through support | **SMS cost per message** once real phones are used (4.3 staging); the ceremony itself costs nothing |
| **4.5 private pilot + migration** — `PILOT.md`: the dry run of both children's v2 exports is done (carried balances, sectors, crowns, inventory, no dropped items, no PIN), the owner's cutover decisions listed (reward store, rocket fuel, no egg pending), the ordered cutover, rollback and exit criteria | real parent/family/child accounts; dry-run comparison of the children's v2 exports; import into the v3 child ids; the pilot family uses v3 — and stops progressing in v2 after cutover | none beyond 4.3 |
| **4.6 device acceptance** — `ACCEPTANCE.md`: the owner's checklist, one expected result per row, three devices | iPhone Safari, Android Chrome, desktop: sign-up, MFA, handover, placement, learning, read-aloud, games, plan changes, recovery, sign-out and failure states | owner's devices and time |
| **4.7 operational and privacy readiness** — `RECONCILIATION.md` (payments) and `PRIVACY.md` (inventory, retention, the parent's rights, what is not collected, what the legal review must settle) written; monitoring/backup/rollback runbooks wait for the staging project | runbooks (payment reconciliation, deletion/export verification, rollback), monitoring, backup assumptions, privacy disclosures and the retention table against the staging infrastructure | none |

## Ground rules

- `PR #1` (release → main) stays **draft**. v2 stays live and untouched until the pilot family has cut over
  and the owner says so.
- The provider adapter keeps the Stage 3 contract (`PAYMENTS.md` → adapter contract). If the contract needs a
  change, that is a Stage 3 hardening PR with the review team, not an adapter shortcut.
- Nothing in Stage 4 may add a browser route that writes arbitrary state; operator work stays in the CLI.
- Every workstream ends with the same gate: unit + emulator + container-build green on the exact release push.

## Order

4.0 → 4.1 → 4.2 (all zero-cost; the owner needs only a free Stripe test account for 4.2) → 4.3 when the owner
opens the billing account → 4.4 → 4.5 → 4.6 → 4.7 alongside.

## Stage 4 review follow-ups (team review of 9 Sep 2026)

| Finding | Closed by |
|---|---|
| **4.0** a provider failure during sign-in account deletion left the identity alive and the door open: a fresh token could sign in and create a new family; sessions were swept once, capped at 200 | `login()` and `authorize()` refuse a parent with `identityDeletion` (`ACCOUNT_DELETED`) from the first marker on; sessions are swept in bounded batches (`sweepSessions`, 300 per transaction); the operator's retry finishes. Tests: fault → no sign-in, no session, a stray session refused, no family; 261 old session rows |
| **4.4** completion read `pending`, then removed the factor without a compare-and-set: a sign-in that cancelled in between lost | a transactional claim `pending` → `completing` immediately before the provider is asked; a cancellation that committed first wins and the provider is never touched; a claim that committed first is the point of no return (a later sign-in cancels nothing, its session goes; the operator cannot cancel). Gated race tests in memory (both orders, and the resume after a provider fault) and against the Auth emulator |
| **4.4** the public recovery forms disclosed whether an account exists (the original ready date; `PROOF_REQUIRED` / `NOT_PENDING` / `WAITING`) | `start` always answers *now + seven days*; `complete` always answers `{ completed: false }` unless it completed; the client copy explains the three conditions instead |
| **4.1** an upgrade committed `plan.change` on Stripe's API answer, even when the proration charge failed or needed authentication (`payment_behavior` default `allow_incomplete`) | `payment_behavior=pending_if_incomplete`; the adapter answers `applied` or `pending`; a pending upgrade is an `awaiting_payment` intent — plan and seats unchanged, marker kept (`PAYMENT_PENDING` for a day), hosted invoice returned — and the matching `invoice.paid` grants it once. Tests: charged on the spot; held; failed invoice; paid invoice applies once; redelivery replays; lapse and supersede |
| **4.1** `charge.refunded` mapped the charge's running `amount_refunded` to a new refund record (200 + 500 recorded for 200 + 300 refunded) | `refund.created` / `refund.updated`: the per-refund object's own amount and id; the charge fetched for the customer and for "refunded in full"; `DUPLICATE_REFUND` on a second delivery of the same refund; `charge.refunded` recorded and ignored. Tests: 200 then 300 → records 200 and 300 |
| **4.1** `invoice.paid` took the first line with a price; a proration invoice lists the old price first | a paid invoice is resolved against the subscription Stripe holds; otherwise the best line (positive amount, latest period). Tests: a two-line proration invoice, paid and failed |
| owner (9 Sep 2026): a routine whole-database sweep, and disputes | `support.inspectAll` / `scripts/support.mjs sweep` (read-only, `sweeps/{id}`, exit 2 on findings) scheduled nightly as a Cloud Run job (`05-sweep-job.sh`); `seatOverflow`, `refundFailures`, `disputesWon` in the family report; `charge.dispute.created` → full refund (access ends when the dispute opens), a refund counts from pending, `refund.failed` and `dispute.won` recorded for the operator |
| **4.5** "nothing dropped" overstated the dry run: the importer carries the last 60 history rows | `PILOT.md` names the 12 older rows not carried (Allison 1, Geralt 11) as the owner's decision, and the raw exports as the archive |
| **4.4** (second round) the claim re-read only `pending`: an attempt that had verified request A could claim a request B started after A was cancelled by the owner's sign-in, or after A lapsed, on A's proof and wait | every request carries an immutable `requestId`; `complete()` binds every step — the lapse, the claim, the completion — to the id it read; a replaced request answers `{ completed: false }` and the provider is never touched; a record without an id never completes. Tests: A verified → owner signs in → B started → the old attempt resumes: B pending with its whole wait, factor enrolled; the same across A's lapse |
| **4.1** (second round) a held upgrade completed as an authorized `payment.succeeded`, which cleared a cancellation the parent made while waiting; and it was matched by family and target plan, so another paid invoice could complete it | the intent records `proration.invoiceRef`; only that invoice's `invoice.paid` completes it, through `plan.change` (cancellation preserved, period not renewed, an ended subscription not revived — `INVALID_TRANSITION` for the operator; another price at the provider — `UPGRADE_PLAN_MISMATCH`). Tests: held → cancel at period end → paid: Family, cancellation intact, Stripe agrees; a renewal on the current price and a paid invoice with the target price but another id grant nothing; a refunded family is not revived |
| owner (9 Sep 2026): the WhatsApp-style SMS resend ladder | `functions/` — an Identity Platform blocking function (`beforeSmsSent`, Cloud Functions 2nd gen, free tier) applying `functions/ladder.mjs`: at once, then 2 min, 15 min, 1 h, 6 h, 12 h, a day before the seventh; a day of quiet starts over; HMAC-keyed `smsLadder` records with a two-day TTL; the browser shows the wait; `06-sms-ladder.sh` deploys and registers it (block F) |
